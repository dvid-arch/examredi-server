# ==========================================
# ExamRedi Server Configuration (Production)
# ==========================================

# Port to run the server on (Render provides this automatically)
PORT=3000

# Environment (development or production)
NODE_ENV=production

# JWT Secrets (CRITICAL: Change these to long random strings in production!)
# You can use `node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"` to generate keys.
JWT_SECRET=replace-with-a-secure-random-key-at-least-64-chars
JWT_REFRESH_SECRET=replace-with-another-secure-random-key-at-least-64-chars

# CORS Configuration
# Comma-separated list of allowed origins (e.g., your Vercel frontend URL)
ALLOWED_ORIGINS=https://examredi.vercel.app,http://localhost:5000

# Rate Limiting
# 15 minutes window in milliseconds
RATE_LIMIT_WINDOW_MS=900000
# Max requests per window
RATE_LIMIT_MAX_REQUESTS=100

# NOTE: The current system uses local JSON files in the `/data` folder.
# On Render, these files are ephemeral and will be reset on every deploy/restart.
# Consider using a database or Render Disks for persistence.
